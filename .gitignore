# 의존성 / 빌드 산출물
node_modules/
dist/
*.ait

# 로컬 데이터 — 언제든 API에서 다시 만든다
*.db
*.db-wal
*.db-shm
log
__pycache__/
*.pyc

# 비밀값
.env
.env.local
*.pem

# 스크린샷 원본은 버전관리 대상이 아니다 (콘솔에 올리면 끝)
shots/_sheet.png
logo/_compare.png
