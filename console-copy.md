# 토스 콘솔 입력값 모음

복사해서 붙여넣는 용도. 반려되어 다시 넣을 때도 여기서 꺼내 쓴다.

---

## 워크스페이스 / 제작자

```
워크스페이스 이름    하루하나
제작자 이름         하루하나
```

제작자 이름은 **앱 설명에 노출되고 내가 만드는 모든 미니앱에 붙는다.**
그래서 `입찰레이더`나 특정 도메인(`공공데이터랩` 등)에 종속되지 않는 이름으로 골랐다.
10자 이내 · 한글/영문/숫자만 · 나중에 변경 가능.

---

## 기본 정보

```
앱 이름(국문)    입찰레이더
앱 이름(영문)    Bidscope
appName         bidscope
앱 유형          비게임
```

⚠️ **`appName`은 등록 후 변경 불가**다. 딥링크(`intoss://bidscope`)와
서버 인증서 발급에 묶인다. `miniapp/apps-in-toss.config.ts`의 값과 반드시 같아야 한다.

---

## 카테고리

```
생활 > 비즈니스 > 사장님
```

사용자가 공공입찰을 따려는 **사업자**다. `공공·행정`은 정부24·민원 같은 앱이
모이는 자리라 일반 시민이 보고, 사업자는 비즈니스 탭을 뒤진다.
카테고리는 나중에 변경 가능하니, 비즈니스가 붐비면 `공공·행정`으로 옮겨 본다.

---

## 부제 (10자)

```
공공입찰 미리 알림
```

`입찰레이더`만 보면 공공인지 민간인지 모른다 → "공공"이 필요.
"미리"는 우리 유일한 차별점(사전규격이 공고보다 중앙값 7일 먼저)이다.

---

## 상세 설명 (432자 / 500자 제한)

```
처음 열면 분야를 고르는 화면이 나와요. ICT 서비스, 기술용역, 시설물관리처럼 공공기관이 발주하는 분야가 버튼으로 나열돼 있어요. 하나를 누르면 세부 분야가 펼쳐지고, 분야마다 최근 공고가 몇 건인지 숫자로 보여줘요. 사업 규모와 계약 방법도 고를 수 있어요.

'이 조건으로 받기'를 누르면 조건에 맞는 공고가 모인 화면으로 넘어가요. 마감까지 남은 시간, 예산, 발주 기관이 한눈에 보이고, 하루도 안 남은 공고는 주황색으로 표시돼요. 공고를 누르면 조달청 나라장터 원문이 열려요.

맨 위에는 '공고 예고'가 따로 모여 있어요. 아직 공고가 나지 않은 사전규격 단계라, 보통 일주일쯤 뒤에 실제 공고가 나와요. 미리 알고 준비할 수 있어요.

'공고 올라오면 알림 받기'를 누르면 앱을 열지 않아도 돼요. 새 공고, 공고 예고, 마감 임박까지 알림으로 알려드려요.
```

68자 여유가 있다. 더 넣고 싶으면 이 문장이 가장 값어치 있다 (+58자):

> 관심 키워드를 적어두면 공고 이름에 그 말이 들어간 것만 찾아줘요.

**의도적으로 뺀 말**: `실시간`(하루 3번 수집이라 거짓), `수익`·`수주`·`낙찰 보장`
(금융·투자 카테고리 오해 방지), `~가 없어요`(가이드가 긍정형을 요구).

---

## 이미지

```
앱 로고          logo/logo-light.png     600×600
다크모드 로고     logo/logo-dark.png      600×600
스크린샷         shots/01-setup.png      636×1048
                shots/02-home.png
                shots/03-notices.png
```

로고는 `python3 logo.py`로 다시 생성 가능(색·형태를 바꾸려면 숫자만 고친다).
스크린샷은 `python3 shot.py <캡처> <출력> --top=0.0`으로 규격을 맞춘다.

---

## 검색 키워드

```
입찰, 나라장터, 공고, 조달, 입찰공고, 공공입찰, 관급, 수의계약, 사전규격, 용역
```

`낙찰`은 뺐다 — 낙찰 정보는 아직 제공하지 않는다.

---

## 고객문의

```
seoyoungjae92@gmail.com
```

---

## 푸시 템플릿 3종 (스마트발송 → 기능성 캠페인)

제목 **7자** / 본문 **25자** 이내(공백 포함) · 해요체 · 사실 서술만.

| 제목 | 본문 |
|---|---|
| `새 입찰공고` | `{키워드} 공고 {n}건 올라왔어요` |
| `공고 예고` | `관심 분야 {n}건 공고 예정이에요` |
| `마감 임박` | `{공고명} {시}시 마감이에요` |

실제 렌더 예시(`server/push.py`가 길이를 강제한다):
```
[새 입찰공고]  SW개발 공고 31건 올라왔어요
[공고 예고]    관심 분야 26건 공고 예정이에요
[마감 임박]    KACADEMY 홈페이지 10:00 마감이에요
```

**금지**: "지금 바로", "놓치면 후회", 감탄사, 이모지, 구독·혜택 유도 문구.
혜택 문구가 섞이면 **광고성으로 재분류**되어 2026-10-01 이후 발송이 막힌다.

**알림동의문도 함께 등록**해야 한다. `Notification.requestAgreement()`가 이걸 참조한다.

승인되면 받은 코드를 Railway 변수에:
```
TOSS_TEMPLATE_NEW=...
TOSS_TEMPLATE_CLOSING=...
TOSS_TEMPLATE_PRESPEC=...
```

---

## 사업자등록 — 지금은 불필요

| 기능 | 사업자등록 |
|---|---|
| 미니앱 출시 | 불필요 (2025-12-09부터) |
| 배너 광고 · IAP(구독) · 토스페이 · 프로모션 · 토스 로그인 | 필수 |

~~배너 광고는 누적 수익 5,000원까지 유예~~ — 틀렸다. 광고 약관 등록에서 사업자번호를 요구한다.
그래서 광고 없이 먼저 출시한다(2026-09-19). 무료 출시라 지금은 막히지 않는다.
나중에 등록할 때 **사업자등록증 업종이 앱 카테고리와 일치**해야 한다는 조건이 있다
(소프트웨어 개발 / 정보서비스업 쪽이 이 앱과 맞다). 정산정보 심사가 별도로 2~3영업일.
