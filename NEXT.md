# 출시 이후 — 입찰레이더를 키우는 순서

작성 2026-09-20. 현재 상태는 [HANDOFF.md](HANDOFF.md), 설계 근거는 [VERIFIED.md](VERIFIED.md)에.

> **전제: 지금은 아무것도 건드리지 않는다.** 심사 중이다. 번들을 바꾸면 심사가 처음부터 다시다.
> 아래는 전부 **출시 승인 이후**의 순서다.

---

## 원칙 — 아이템을 늘리기 전에 하나가 되는지 본다

리텐션이 안 나오면 카테고리를 늘려도 똑같이 안 나온다. 공고량을 3배로 늘려도
"알림을 받고 다시 들어오는 사람"의 비율은 그대로다. 순서를 지킨다.

또 하나. **토스는 같은 워크스페이스에서 핵심 기능이 유사한 앱을 복수 출시할 수 없다.**
지원사업·공매 같은 "공고 알림"류는 **별도 앱이 아니라 이 앱의 카테고리**로 넣어야 한다.
(출처: 앱인토스 서비스 오픈 정책. 실제로 "유사 앱으로 걸려 정지된 앱" 사례가 있다.)

---

## 0단계 — 출시 직후 (코드 변경 없음)

- [ ] 콘솔에서 **'핵심 지표'** 설정. 토스 추천 알고리즘이 lookalike 기반이라
      **활성 지표의 정의가 곧 노출량**이다. `앱 실행`처럼 헐겁게 잡으면 닮은꼴 모집단이 뭉개진다.
      → **"조건 등록 완료"** 또는 **"알림 동의 완료"**처럼 가치를 느낀 순간으로 잡는다.
- [ ] 앱 이름·부제에 **검색어**를 넣는다. 혜택탭 유입은 토스 정책 한 줄에 증발하지만
      (2026년 1월 7만명대 → 5월 3만명대 실측) 검색은 사용자 의도 기반이라 덜 흔들린다.
      한 미니앱은 제목만 바꿔 검색 유입 2.2배를 얻었다.
- [ ] **2026-09-30 시작되는 비게임 전수 점검** 대비. 통보 후 14일 내 미승인 시 노출 중단.
- [ ] 첫 푸시 수신 확인 → 이전 mTLS 인증서 `bidscope` 폐기.

## 1단계 — 심사 반려 위험 제거 (다음 업데이트에 반드시 포함)

- [x] **알림 끄기** (2026-09-22). 동의는 켤 수만 있고 끌 수 없었다 — 서버·API는 `agreed:false`를
      받을 준비가 돼 있는데 화면에서 부르는 곳이 없었다. 조건 목록 아래 종류별 토글로 연결했다.
      SDK에 동의 해제 함수가 없어서 끄기는 서버에만 기록하고, 켜기는 동의 시트를 다시 띄운다
      (이미 동의한 사용자는 `alreadyAgreed`로 바로 켜진다).

- [ ] **공고 탭 시 외부 이동을 사용자가 인지하게 한다.**
      지금은 누르면 안내 없이 나라장터로 나간다. "사용자가 인지하지 못한 외부 페이지 이동"은
      **6회 연속 반려 사례**가 있는 항목이다. 버튼에 `나라장터에서 보기 ↗` 라벨을 붙인다.
- [ ] **공고 예고(사전규격)는 페이지가 아니라 첨부파일 링크**(`specDocFileUrl1`)다.
      파일이 없는 건은 눌러도 아무 일이 없다 — 링크 없는 항목은 누르지 못하게 하거나 이유를 표시한다.

## 2단계 — 업무유형 확장 (가장 값싼 성장)

지금은 **용역 하나만** 본다. 하루 780건을 버리고 있다.

| 업무유형 | 하루 공고량 | 분류체계 | 상태 |
|---|---|---|---|
| 물품 | 450건 | **전부 비어 있음** (표본 300건) → 키워드 매칭만 가능 | 미수집 |
| 용역 | 423건 | 채워져 옴 → 2단계 선택이 주 매칭축 | ✅ |
| 공사 | 326건 | **미확인 — 착수 전 표본 확인 필요** | 미수집 |
| 외자 | 5건 | — | 무시 |

수집 자체는 환경변수 한 줄이다: `BIDNOTE_TYPES=용역,공사` (`server/g2b.py`의 `ACTIVE_TYPES`).
**진짜 일은 매칭축이다.**

- [ ] **공사부터.** 분류가 채워져 오는지 표본 300건으로 확인한다. 채워지면 용역과 같은 UX로 끝난다.
- [ ] **물품은 그다음.** 분류가 없으니 조건 등록 화면이 달라져야 한다(키워드 중심).
      `purchsObjPrdctList`, `pubPrcrmntClsfcNo` 필드로 대체 축이 되는지 먼저 확인.
- [ ] 업무유형이 늘면 조건 등록 첫 단계에 **업무유형 선택**이 생긴다. 지금은 용역 고정이다.

사용자층이 "1인 사업자"에서 **건설업·납품업**까지 넓어진다.

## 3단계 — 6주 관찰 (확장을 멈추고 본다)

문서에 이미 적어둔 계획대로 **등록 사용자·리텐션만** 본다. 볼 것:

- 조건을 등록한 사람 중 **알림 동의**까지 간 비율
- 푸시를 받고 **다시 들어온** 비율 (이게 제품의 생사다)
- 조건 3개를 다 쓴 사람의 비율 → 구독 전환 가능성의 선행 지표

## 4단계 — 앱 안에 머무는 이유 만들기

지금은 공고를 누르면 즉시 앱 밖으로 나간다. 세션이 짧아 추천 알고리즘에도 불리하고
나중에 광고를 붙여도 노출이 안 난다.

- [ ] **공고 요약 시트**: 발주기관·예산·마감일·계약방법을 앱 안에서 먼저 보여주고,
      볼 만할 때만 "나라장터에서 보기"로 넘긴다. 1단계의 반려 위험 제거와 같이 하면 일석이조다.
- [ ] 조건 수정 기능 (지금은 삭제 후 재등록)

## 5단계 — 카테고리 확장: 지원사업 공고

입찰공고와 **사용자도 구조도 같다**(마감 있는 공고 + 조건 매칭 + 미리 알림).
별도 앱으로 내면 유사 앱 규정에 걸리니 **이 앱 안의 카테고리**로 넣는다.

- 데이터: 중소벤처기업부 기업마당 지원사업 공고 API. **인증키 없이 공개.**
  실측(2026-09-20) 신청 가능 1,528건 · 주간 신규 204건 · 최신 5건이 모두 2일 내 등록.
- 빈틈: **기업마당 앱에는 푸시 알림 기능이 아예 없다.** 사이트에도 개인화 구독이 없다.
- 필터축: `pblancNm` `bsnsSumryCn` `reqstBeginEndDe`(신청기간) `trgetNm`(지원대상)
  `pldirSportRealmLclasCodeNm`(분야) `hashTags` `jrsdInsttNm`.
  ⚠️ **지역 전용 필드가 없다** — 공고명의 `[전북]` 접두어를 파싱해야 한다.
- ⚠️ 포털판은 공공누리 **제3유형(변경금지)**. 원문 그대로 노출 + 출처 표시면 무방하나
  요약·재작성 기능을 붙이려면 자체 API 이용조건을 운영기관에 확인할 것.
- ⚠️ **타깃은 소상공인이 아니라 창업기업으로 잡는다.** 소상공인 지원은 단건 수십만원이지만
  창업 사업화 과제는 수천만원이다. 자릿수가 두 배 차이 나고, 이게 구독가를 정당화하는 유일한 축이다.
- ⚠️ **융자(정책자금) 공고는 뺀다.** 2025년 기준 3조 7,700억으로 이 카테고리의 절반이지만
  앱인토스는 **금융상품(대출 비교)을 완전 금지**한다. 넣으면 심사에서 걸린다.
- ⚠️ **정부가 직접 죽이러 온다.** 기업마당+중소벤처24 통합 플랫폼이 구축 중이고
  2,700여 공고 통합 + **AI 업종·지역·이력 기반 자동 추천** + 사업계획서 자동 생성까지 포함한다.
  출시되면 이 기능 전체가 무료 정부 서비스의 부분집합이 된다. **선점하거나 하지 말거나다.**
- ⚠️ 경쟁: **캐시노트가 140만 사업장**(자영업자 4명 중 1명)에 200여 지원정책을 이미 제공한다.
  중기부도 2026-03부터 수신동의 81만 명에게 알림톡을 보낸다(단 **사용자가 조건을 못 고른다**).
  우리의 차별점은 "내가 조건을 정한다"뿐이다. 이게 충분한지 3단계 데이터로 판단한다.

## 6단계 — 수익화

- **광고**: 사업자등록 없이 **누적 예상수익 5,000원까지** 유예된다. 시장 검증 도구로만 쓴다.
  단 콘솔 약관 등록 화면이 사업자번호를 요구한 적이 있다(2026-09-19) — 채널톡 확인 필요.
- **광고 단독은 답이 아니다.** 한 개발자 6주 실측: 노출 3.3배 증가, eCPM 73% 하락, 수익 정체.
  2026년 배너 eCPM은 1,000원 미만대다.
  역산하면 **광고만으로 월 50만원을 만들려면 MAU 약 17만 5천명**이 필요하다.
  인앱광고 2.0(2025-10) 이후 eCPM이 47.9% 떨어졌고, 외부 광고 네트워크가 금지돼 광고주 풀이 고정이라
  **미니앱이 늘수록 단가가 구조적으로 하락한다**(2026-02 1,000개 → 04월 2,053개).
- **구독으로 간다.** 수수료가 20%로 고정(앱마켓 15% + 토스 5%)이고, 공개된 최대 매출 사례도 구독이다.
  과금 축은 조건 개수가 아니라 **참가자격 판정·낙찰 정보** 쪽이 유력하다(round2-review.md).
  사업자등록은 이 시점에 필요하다(업종: 소프트웨어 개발 / 정보서비스업).

---

## 참고 — 이 아이템이 다른 후보보다 나은 이유

2026-09 조사에서 공공데이터 알림 아이템 7종(지원사업·특허·채용·농수산물·자격증·리콜·경매)을
모집단·지불의사·침투율로 역산했는데, **중립 시나리오에서 월 50만원을 넘는 것이 하나도 없었다.**

그 조사가 찾아낸 법칙 하나가 이 제품의 위치를 설명한다:

> 정보 알림에 돈을 내는 사람은 **그 정보로 돈을 버는 사람**뿐이다.
> 그 정보로 돈을 아끼거나 리스크를 피하는 사람은 내지 않는다.

입찰은 전자다. 인접 시장에 **20년 이상 유지된 유료 구독**이 실재한다
(케이비드 12개월 286,000원, 비드프로 2002년~). 7개 후보 중
정책·API 라이선스·이벤트 빈도·지불의사를 **동시에** 만족하는 것은 하나도 없었다.
→ 새 아이템을 찾기 전에 이걸 끝까지 밀어보는 게 맞다. 상세는 `~/Desktop/workspace/app-ideas/README.md`.

## 하지 말 것

- **공매(온비드) 추가** — 데이터는 깨끗하고 필터축도 훌륭하지만 토스가 **"경매/공매"를 명시적으로 금지**한다.
  부동산 카테고리가 통으로 막혀 있다(시세 조회·청약·상권 분석까지).
- **별도 앱 양산** — 같은 워크스페이스 유사 기능 복수 출시 금지.
- **리워드·포인트 미끼형 유입** — 2026-09 광고 어뷰징 배분 기준 도입으로 직격탄.
