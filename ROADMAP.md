# 출시까지 로드맵

준비물: [PREP.md](PREP.md) · 검증: [g2b_check.py](g2b_check.py) · 근거: [round2-review.md](round2-review.md)

---

## 0단계 — 아직 안 했으면 여기부터 (게이트)

```bash
export G2B_KEY='디코딩_인증키' && python3 g2b_check.py
```

**키워드별 월 30건이 안 나오면 이름 짓지 말고 접는다.** 이름·계정·개발은 전부 이 뒤의 일이다.

---

## 1단계 — 이름 확정 ⚠️ appName은 나중에 못 바꾼다

### 토스 규칙 (공식 문서)

| 항목 | 규칙 | 수정 |
|---|---|---|
| **appName** | 딥링크 키 `intoss://{appName}`. 서버 인증서 발급에 묶임 | **✗ 불가** |
| 앱 이름(국문) | 토스 앱에 노출. 검색에 걸리는 이름 | ○ 가능 |
| 앱 이름(영문) | **15자 이내**, 발음하기 쉬운 **명사형**. 명령문·동사구 불가. 일반 단어 단독 불가(**브랜드명 형태 필수**). 단어 3개 이상·복합어 피할 것 | ○ 가능 |
| 제작자 이름 | 공백 포함 **10자 이내**, 한/영/숫자 | ○ 가능 |
| 워크스페이스 이름 | 사업자당 1개, 중복 불가 | — |

### 추천안

| | 안 A (추천) | 안 B | 안 C |
|---|---|---|---|
| 국문 | **입찰알림** | 입찰레이더 | 오늘의입찰 |
| 영문 | **Bidnote** (7자) | Bidcast (7자) | Bidly (5자) |
| appName | **`bidnote`** | `bidcast` | `bidly` |

**안 A를 권한다.** 국문명에 "입찰"이 들어가야 토스 검색에 걸린다. 영문은 브랜드형 명사 한 단어라 규칙을 안전하게 통과한다.

**쓰면 안 되는 이름:** `bidpro`, `bidmate`, `kbid`, `ebid` — 전부 기존 유료 경쟁사(비드프로·비드메이트·케이비드·이비드코리아) 상표다. `nara`/`narajangteo`도 조달청 서비스명이라 피한다.

**결정 전 확인:** 토스 앱에서 후보 이름을 검색해 중복이 없는지 본다.

---

## 2단계 — 콘솔 가입 & 앱 등록 (1~2영업일)

```
https://developers-apps-in-toss.toss.im/
```

토스 비즈니스 회원 기반, 만 19세 이상. **사업자등록 불필요**(2025-12-09부터).

### 등록 순서
1. 회원가입 → **워크스페이스 생성** (이름 + 제작자 이름 10자 이내)
2. **미니앱 등록**
   - 앱 이름 국문/영문, **appName**(← 신중히), 앱 유형 **비게임**
3. **앱 정보 입력**

### 준비해둘 것

| 항목 | 규격 |
|---|---|
| 앱 로고 | **600×600 PNG**, 정사각형, **둥근 모서리 불가**, 배경색 필수 |
| 스크린샷(선택) | 세로 636×1048 / 가로 1504×741 PNG |
| 부제 | 10자 이내 — 예: `내 조건 입찰공고 알림` |
| 상세 설명 | 핵심 기능까지의 **사용자 경험 흐름**으로 |
| 사용 연령 | 전체 |
| 고객문의 이메일 | 실제 받는 주소 |
| 카테고리 | 비게임. 1단계 검증 때 본 카테고리 목록에서 선택 |

---

## 3단계 — 개발 (2~3주)

```bash
npx ait init          # WebView SDK 3.x — 2.x는 롤백 불가
npm run dev           # AIT Devtools로 브라우저에서 바로 테스트
```

### 제약 (심사 체크리스트와 직결)
- **CSR만** (SSR 금지), iframe 금지, `eval` 금지, https/wss만
- 라이트 모드 필수, 토스 네비게이션바 사용
- 번들 **100MB 이하**(압축 해제 기준)
- 서버 CORS 허용: `https://{appName}.apps.tossmini.com`, `https://{appName}.private-apps.tossmini.com`

### 최소 기능 (MVP)
| 화면 | 내용 |
|---|---|
| 온보딩 | 첫 화면이 곧 조건 등록. 업종·키워드·지역 선택 |
| 내 조건 | 등록한 조건 목록, 매칭 공고 수 |
| 공고 리스트 | 최근 매칭 공고. 마감 임박순 |
| 공고 상세 | 공고명·기관·마감일시·기초금액 + 나라장터 원문 링크 |
| 알림 설정 | 조건별 on/off, 알림 동의 |

서버는 단일 VM($13/월) + Postgres. 증분 조회가 되므로 스냅샷 diff가 빠져 1차 설계보다 단순하다. 구조는 [apt-alert-appendix.md](apt-alert-appendix.md) §D 재사용.

---

## 4단계 — 푸시 템플릿 검수 (2~3영업일, 개발과 병행)

콘솔 **스마트발송 → 기능성 캠페인**. 제목 **7자** / 본문 **25자** 이내(공백 포함), 해요체.

```
제목: 새 입찰공고    본문: {키워드} 공고 {n}건 올라왔어요
제목: 마감 임박      본문: {공고명} 오늘 {시}시 마감이에요
제목: 주간 요약      본문: 이번 주 내 조건 공고 {n}건이에요
```

**알림동의문도 함께 등록.** 기능성 푸시는 `Notification.requestAgreement()` 동의가 필요하다.

금지: "지금 바로", "놓치면 후회", 감탄사, 이모지, 구독 유도. 혜택·유도가 섞이면 광고성으로 재분류돼 **2026-10-01 이후 발송 불가**가 된다.

---

## 5단계 — 테스트 (필수, 안 하면 검토 요청 버튼이 안 열림)

```bash
npm run build         # .ait 번들 생성
```

콘솔에 번들 업로드 → **QR 코드로 실기기 테스트 최소 1회**.
테스터는 워크스페이스 멤버, 만 19세 이상, 토스 로그인 필요.

`send-test-message`로 푸시도 실제 폰에서 받아본다.

---

## 6단계 — 검토 요청 & 출시

콘솔 **검토 요청** → 영업일 **최대 3일**(카테고리에 따라 7일+). 반려 시 수정 후 새 번들 업로드하고 재요청.

승인되면 이메일이 오고, **출시하기** 버튼을 누르면 **즉시 전체 사용자에게 반영**된다.

### 심사 체크리스트 (지금 비게임 전체 점검 중이라 더 빡빡하다)
- [ ] **뒤로가기 버튼 정상 동작** ← 점검 핵심 항목
- [ ] **테스트용 광고 키 제거** ← 점검 핵심 항목
- [ ] 10초 내 최초 화면 로드
- [ ] 앱 설명에 쓴 기능이 미니앱에서 전부 동작
- [ ] 자사 로그인 없음 (토스 로그인만)
- [ ] 라이트 모드 / 토스 네비바 / CSR / eval 없음
- [ ] 진입 직후 바텀시트 없음, 알림 동의 선요구 없음
- [ ] 다이얼로그 왼쪽 버튼은 "닫기"

알려진 반려 사유: 앱 이름 한글 표기 문제, 제출 이름 불일치, 구 SDK 번들, 미완성 기능.

---

## 7단계 — 출시 후: 검증 6주

무료로 운영하며 **등록 사용자 / 리텐션 / 조건 등록률**만 본다.

'핵심 지표'를 콘솔에서 설정한다 — 토스가 이 지표로 유사 유저에게 추천하므로 **무료 유입 = 리텐션의 함수**다. 입찰 알림은 "주 1회 이상 방문" 정도가 적절.

---

## 8단계 — 수익화 (검증 통과 후에만)

| 순서 | 항목 | 소요 |
|---|---|---|
| 1 | 사업자등록 (소프트웨어 개발 / 정보서비스업) | 1~2영업일 |
| 2 | 정산정보 등록 | 2~3영업일 |
| 3 | IAP 자동갱신 구독 상품 등록 | — |

- 월 4,900원 / 연 39,000원, **무료체험 7일**(3일은 취소의 55%가 Day 0)
- **하드 페이월** (전환 10.7% vs 프리미엄 2.1%)
- 체험 시작 즉시 "최근 7일 내 매칭 공고 N건" 푸시 → 첫날 가치 전달
- 해지 경로 명시 (iOS/Android 구독 관리) — 전자상거래법 다크패턴 규정

**활성 구독 126명 = 월 50만원.**

---

## 전체 타임라인

| 주차 | 할 일 |
|---|---|
| **0주** | 검증 스크립트 ← 여기서 접을 수 있다 |
| **1주** | 이름 확정, 콘솔 가입·앱 등록, 로고 제작 |
| 2~3주 | 서버(수집·매칭) + 미니앱 개발. 푸시 템플릿 검수 병행 |
| 4주 | 번들 업로드, QR 테스트, 검토 요청 |
| 5주 | 심사 → 출시 |
| 6~11주 | 무료 운영, 리텐션 검증 |
| 12주~ | 사업자등록 → IAP 구독 |
