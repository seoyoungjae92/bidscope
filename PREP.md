# 나라장터 입찰 알림 — 준비 체크리스트

결정: 나라장터로 간다. 근거는 [round2-review.md](round2-review.md).
검증 스크립트: [g2b_check.py](g2b_check.py)

---

## 지금 당장 (30분, 순서대로)

### 1. 공공데이터포털 API 신청 — 리드타임 있음, 제일 먼저
```
https://www.data.go.kr/data/15129394/openapi.do
```
- 회원가입 → **활용신청** → 자동승인(심의 없음)
- 활용목적 아무거나("개인 서비스 개발"), 상세기능 **전체 선택**
- **키 활성화까지 10분~최대 1영업일.** 신청부터 해두고 나머지 진행

발급 후 `마이페이지 > 오픈API > 인증키`에서 **일반 인증키(Decoding)** 복사. Encoding 키가 아니라 **Decoding 키**다. 스크립트가 알아서 인코딩한다.

### 2. 참고자료 문서 다운로드
같은 페이지 하단 **`조달청_OpenAPI참고자료`** — 오퍼레이션별 파라미터·응답 필드 전체 명세가 웹페이지엔 없고 이 문서에만 있다. 필드명 확인할 때 계속 본다.

### 3. 검증 스크립트 실행
```bash
export G2B_KEY='발급받은_디코딩_인증키'
python3 g2b_check.py
```
stdlib만 쓴다. 설치할 거 없음. 약 50건 호출(한도 1,000건/일).

---

## 스크립트가 확인하는 것

| # | 항목 | 통과 기준 |
|---|---|---|
| 1 | 업무유형 4종(물품/용역/공사/외자) 오퍼레이션 생존 + 전체 물량 | 최소 2종 응답 |
| 2 | 응답 필드 충족률 — 공고명·마감일시·기초금액·상세URL이 실제로 채워져 오나 | 푸시 문구에 쓸 필드가 90%+ |
| 3 | **키워드별 공고 밀도** ← 본 판정 | **월 30건 이상인 키워드가 40% 이상** |
| 4 | 시간 단위 증분 조회 동작 (오전+오후 = 종일) | 일치 → 스냅샷 diff 불필요 |

**판정이 "중단"으로 나오면 거기서 접는다.** 아파트 실거래가에서 배운 것 — 밀도 없는 알림은 제품이 안 된다.

키워드는 스크립트 상단 `KEYWORDS` 리스트를 바꿔가며 여러 번 돌려라. 타깃 업종을 좁힐 수 있는지 보는 게 목적이다.

---

## 병행 — 발견성 검증 (코드 없이, 토스 앱에서)

데이터가 좋아도 아무도 못 찾으면 소용없다. 이게 나라장터의 가장 큰 리스크다.

- [ ] 토스 앱 → 전체 → 미니앱 홈. **카테고리 목록**을 본다. 입찰/사업자 대상 앱이 들어갈 카테고리가 있나?
- [ ] **머니체크 화물용**, **용달 비용 계산기** 검색 → 어느 카테고리에 몇 위인지 확인. **이게 B2B 니치 앱 천장의 실측이다**
- [ ] "입찰", "나라장터", "공고" 검색 → 경쟁 미니앱 유무 (커뮤니티 원문엔 0건이었지만 앱 스토어는 직접 봐야 함)
- [ ] 소상공인·자영업 커뮤니티에서 기존 유료 서비스(비드프로·케이비드) 후기와 불만 검색

---

## 토스 쪽 준비 (검증 통과 후)

### 4. 개발자 콘솔 가입 + 앱 등록
```
https://developers-apps-in-toss.toss.im/
```
- 토스 비즈니스 회원 기반, 만 19세 이상
- **사업자등록 불필요** — 2025-12-09부터 사업자 없이 출시 가능
- 필요: 앱 국/영문명, `appName`(딥링크 키, 영문 소문자+하이픈), 카테고리, 로고 **600×600 PNG**, 스크린샷, 설명, 연락 이메일
- 검토 **1~2영업일**

### 5. 개발 환경
```bash
npx ait init            # WebView SDK 3.x
```
- **반드시 3.x** — 2026-09-14부터 필수, 2.x는 롤백 불가
- CSR만(SSR 금지), iframe 금지, `eval` 금지, https/wss만
- 라이트 모드 필수, 토스 네비게이션바 사용
- AIT Devtools로 브라우저에서 바로 테스트 가능(3.0.2+)
- 서버 CORS에 `https://{appName}.apps.tossmini.com`, `https://{appName}.private-apps.tossmini.com` 허용

### 6. 푸시 템플릿 검수 제출 — 리드타임 2~3영업일
콘솔 **스마트발송 → 기능성 캠페인**. 제목 **7자**, 본문 **25자** 이내(공백 포함), 해요체.

초안 3종:
```
제목: 새 입찰공고        본문: {키워드} 공고 {n}건 올라왔어요
제목: 마감 임박          본문: {공고명} 오늘 {시}시 마감이에요
제목: 주간 요약          본문: 이번 주 내 조건 공고 {n}건이에요
```
금지: "지금 바로", "놓치면 후회", 감탄사, 이모지, 구독 유도 문구. 혜택·유도가 섞이면 **광고성으로 재분류**돼 2026-10-01 이후 발송 불가가 된다.

**알림동의문 등록도 함께.** 기능성 푸시는 `Notification.requestAgreement()` 동의가 필요하다.

### 7. 심사 체크리스트 (비게임 전체 점검 진행 중, 2026-09-30~)
- [ ] 뒤로가기 버튼 정상 동작
- [ ] **테스트용 광고 키 제거** ← 이 둘이 점검 핵심 항목
- [ ] 10초 내 최초 화면 로드
- [ ] 라이트 모드 / 토스 네비바 / CSR / eval 없음
- [ ] 자사 로그인 없음 (토스 로그인만)

2026-08-18 기준 강화가 **기존 앱에도 소급 적용** 중. 심사 최대 1주.

---

## 나중에 (결제 붙일 때)

무료 버전으로 등록 사용자·리텐션을 먼저 검증한 뒤에 한다. **순서를 지켜라.**

- [ ] **사업자등록** — 업종: 소프트웨어 개발 또는 정보서비스업. 심사 1~2영업일
- [ ] 정산정보 등록 — 2~3영업일
- [ ] IAP 자동갱신 구독 상품 등록 (월 4,900원 / 연 39,000원, **무료체험 7일**)
  - 3일 체험은 취소의 55%가 Day 0에 발생한다. 7일로 간다
  - **하드 페이월** (전환 10.7% vs 프리미엄 2.1%)
  - 체험 시작 즉시 "최근 7일 내 매칭 공고 N건" 푸시 → 첫날 가치 전달
- [ ] 해지 경로 명시 (iOS/Android 구독 관리) — 전자상거래법 다크패턴 규정

---

## 리드타임 정리

| 항목 | 소요 | 언제 |
|---|---|---|
| data.go.kr 키 활성화 | 10분~1영업일 | **지금** |
| 토스 앱 등록 검토 | 1~2영업일 | 검증 통과 후 |
| 푸시 템플릿 검수 | 2~3영업일 | 개발 시작할 때 |
| 미니앱 출시 심사 | 최대 1주 | 완성 후 |
| 사업자등록 + 정산정보 | 3~5영업일 | 결제 붙일 때 |

---

## 지금 안 해도 되는 것

- 사업자등록 (검증 전에 하지 마라)
- 서버 (검증 끝나고. 단일 VM $13/월, 아키텍처는 [apt-alert-appendix.md](apt-alert-appendix.md) §D 재사용)
- 도메인
- 운영계정 트래픽 증설 (개발계정 1,000건/일로 충분. 활용사례 등록 후 신청)
